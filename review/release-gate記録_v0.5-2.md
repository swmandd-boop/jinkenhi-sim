# リリースゲート記録：人件費トレードオフ・シミュレーター v0.5（第2回・差分）

- 日付：2026-07-28（**レビューのみ・push も公開操作もしていない**）
- 判定：**🟡 条件付き可**（P0なし。P1が1件＝下記 G1「公開済みである」という現況の扱い）
- 位置づけ：本記録は**承認そのものではなく、水田さんが公開ボタンを押すための材料**。
- 種別：`review/release-gate記録_v0.5.md`（第1回・コミット 93568a1）**以降の全変更の差分レビュー**。
  対象：93568a1..HEAD（573b1fb〜58af508 の10コミット、index.html 596行差分ほか）。

---

## G1【最重要・P1】前提が変わっている：**このツールは既に公開されている**

- 第1回の記録は release_context に「配布チャネル：GitHub Pages ※**未公開**」と書き、
  判定も「公開ボタンを押す前の材料」として組み立てていた。
- **現況（2026-07-28 実測）**：`origin/main` == `HEAD`（未push 0件）。
  `https://swmandd-boop.github.io/jinkenhi-sim/` を取得したところ **v0.5 の実物が配信されている**
  （eyebrow「SWMD / Personnel Cost Trade-off v0.5」、職種表は正規・非正規・派遣の列、
  案B適用後の「配置比率・基準線は表示せず」の文言、フッターの forms.gle リンクを確認）。
- したがって本記録は**公開前ゲートではなく、公開済みのものに対する事後レビュー**である。
  第1回で「公開の前提として水田さんの判断が必要」としたP1（§0シート代替など）は、
  **判断を経ないまま公開された状態**になっている可能性がある。
- **必要な決定**：(a) このまま公開継続でよいか、(b) 下記P2/P3を直してから改めて周知するか、
  (c) 一時的に取り下げるか。**ツール自体に公開不可の欠陥は見つかっていない**ため、
  実務的には (a) または (b) を想定するが、**「未公開のつもりだったか否か」は水田さんにしか判断できない。**

---

## STAGE 1 機械チェック（scripts/scan_tool.py ＋ 直接 grep）

- **外部依存：1件（新規）**。フッターの問い合わせリンク `https://forms.gle/...`。
  第1回は「HTML内の `https?://` は SVG名前空間URIのみ」だったので、**状態が変わった**。
  評価：これは**利用者のクリックで開くリンク**で、ページ読込時に外部へ要求を出す資源ではない。
  ページはオフラインでも動作する（リンク先だけが要通信）。→ P3（下記④'の文言整合）。
- APIキー・トークン／localStorage・Cookie／トラッカー／console への入力値出力：**検出なし**。
- 個人情報の入力欄：**なし**（集計値のみ。利用者名・職員名の欄は存在しない）。
- E-001 網羅主張4件：「すべての職種で基準以上」等の**計算結果の条件付き判定文**と
  engine 内コメント1件。テスト（INV/UI/explore）で裏づけ済み。未検証の網羅主張ではない。
- **未検出（P3・第1回から継続）**：「個人情報を入力しない旨」。
  第1回 STAGE4 の公開明示文（案）には「個人情報は入力しないでください」が入っていたが、
  **実装された免責一文（238文字）には含まれていない**（`grep -c 個人情報 index.html` → 0）。

## STAGE 1.5 品質保証フレームワーク照合

- `npm run check` **62件 PASS**／探索 **10,000ケース×15不変条件 違反0**。
  第1回時点は46件だったので +16件（ZERO-01〜04・AXB-01〜03・UI-15〜18・DIL-04 ほか）。
- **変更影響の宣言**：`docs/v0.5-設計.md` に §10（計算式変更時の洗い出し手順）・§11（案B）・
  §12（未検証数値の削除とレイアウト）・§12.3（法令とツールの仮定の書き分け）を追記。
  触った不変条件と追加検査の対応が節ごとに記録されている。
- **DOM抽出突合は E-009 準拠**：全テストが DOM 要素を id/セレクタで特定して `dispatchEvent` で駆動。
  座標クリックは使っていない。実ピクセル検査（LAYOUT×5）も要素の実測値で判定。
- **fail-before の実証**：ZERO-01/02/03（ゼロ除算）と LAYOUT-1280（レイアウト移動）は
  **修正前コミットで落ちることを確認済み**。テストが当該不具合を実際に捕捉することの裏づけがある。
- §0 不変条件シートは形式文書としては未整備（既存テスト＋探索を代替とする方針を継続。第1回で承認済み）。

---

## STAGE 2 観点レビュー（重点4点）

### 重点① A一本化で旧定義の説明が残っていないか … 🟢
- **独立に再走査した**（指紋：`給与原資 ÷（正規` / `職員1人あたり給与費（額面）` / `原資÷人数` /
  `原資÷n` / `月例給` / `賞与月数` / `職員1人あたり人件費`）。
- **本体 index.html：旧定義の残存なし。** README のヒット2件は
  「額面ベースのB…と月例給・賞与月数入力は**削除**」「v0.5 で…**削除した**」という
  **変更履歴の記述**であり、現況を誤って説明しているものではない（§10の分類②）。
- 第1回で見落としていた aria-label・engine コメント・README の検証状態表も是正済みであることを確認。
- リスク残：低。ただし**この確認が成立するのは §10 の走査手順が守られる限り**であり、
  手順自体が守られたかは各変更のレビュー報告に走査結果が貼られているかで判断する必要がある。

### 重点② 通所介護で比率を出さなくなって判定が弱くなっていないか … 🟢
実測（jsdom・通所介護）：

| 確認 | 結果 |
|---|---|
| 比率での基準判定 `ratioBad` | **元から常に false**（`ratio.std = null`）。比率で判定していた事実がない |
| 初期状態 | 「職種別の基準は満たしています」 |
| 定員 35→60 | 「職種別の基準を**下回っています**」＋「介護職員 4.6人不足」＋**基準の内訳を表示** |
| 看護職員を0人に | 「職種別の基準を**満たせません**」（blocked＝看護職員） |
| 営業日数 6→3日 | 介護の基準 4.4 → 2.2（**提供時間・営業日数が基準に効いている**） |

- **判定は弱くなっていない。** 通所の人員基準は元々「15人まで1・超過5人ごとに+1（提供時間帯を通じて）」
  の職種別判定だけで行っており、比率セルは**判定に一切使われていない記述統計**だった。
  むしろ比率という無意味な数字が消えたことで、判定の所在（充足判定パネル）が一本化された。
- リスク残：低。

### 重点③ 免責一文と問い合わせ導線が移動後も見落とされない位置か … 🟡
実ピクセル実測：

| 幅 | ページ高 | 免責/問い合わせの位置 | グラフとの前後 |
|---|---|---|---|
| 1280px | 4501px | y=2423（**54%地点**） | グラフ(354)より後 |
| 390px（スマホ） | 9617px | y=2486（**26%地点**） | **グラフ(3041)より前** |

- 移動前はページ最下部（実質100%地点）だったため、**見落としにくさは明確に改善**している。
- 指摘A（P3）：**スマホでは免責・問い合わせが結果（グラフ）より前に来る**。
  見落とし防止としては有利だが、閲覧順が「入力 → 免責 → 結果」になり、
  結果を見た直後に免責が目に入る導線ではなくなった。表現・配置の調整余地。
- 指摘B（P3・第1回から継続）：免責一文に**「個人情報を入力しないでください」が無い**（上記STAGE1）。
  入力欄自体に個人情報の項目はないため実害は小さいが、第1回の明示文（案）からは脱落している。
  修正案：免責一文の末尾に1文追加。
- リスク残：低〜中（スマホの閲覧順は要確認）。

### 重点④ 未検証の数値提示が本当に残っていないか … 🟢
- **独立に再走査した**（指紋：`程度` / `ことが多` / `おおよそ` / `概算` / `目安` / `18〜19` /
  `とされ` / `一般に` / `通常`）。
- 「18〜19％程度になることが多く」は**削除済み**（値ではなく「決算書の『法定福利費』を『給与費』で
  割れば自法人の実際の負担率が出ます」という確かめ方に置換）。
- 残存ヒットの分類：
  - 「既定の16.5％は…事業主負担分の**おおよその合計**」＝ 既定値が何の合計かの**内訳説明**。
    別途の目安を提示していない → 該当せず
  - 「老健の…は**標準（目安）**」＝ **制度上の「標準」**。こちらの推測ではない → 該当せず
  - `とされ` のヒットは HTML コメント（「見落と**され**にくくする」）の誤ヒット → 該当せず
- ユニット型の「日中16時間・夜間8時間・週7日」は**計算に必要な前提パラメータ**として残置。
  **法令とツールの仮定を分けて表示**する形に改めた（職種表の行注記・caveat・engine コメントの3か所）。
  水田さんの判断（2026-07-28）どおり「未検証」の明記で足りるとし、書き分けで正確性を上げた。
- リスク残：低。

---

## STAGE 2'（その他の観点・差分のみ）

### ②' 制度・法令適合 … 🟡（第1回から改善、残件は同じ）
- **改善**：通所介護に対利用者の比率基準が存在しないことを明示し、比率・基準線を出さなくした（案B）。
  老健の「看護2/7・介護5/7は標準（目安）で行別判定はこれに基づく」を caveat に追加。
  ユニット型の法令／ツール仮定の書き分けも追加。**制度の記述精度は上がった。**
- 残件（P2・第1回から継続）：特養・ユニットの**管理栄養士が定員によらず一律1.0**。
  老健は定員100判定なのに特養は一律で、要約として不正確な可能性。制度精査＝有識者確認が要る領域。
- 残件（P2・開示済み）：兼務可職種の満額計上。注意書き＋不足内訳で開示済み。

### ③' 誤回答・誤判定 … 🟢（差分で改善）
- **ゼロ除算の修正**：公開版で配置比率が `6935543426150564800 : 1` と表示され描画が崩れる不具合を修正。
  原因は(1)通所で軸域を現在値から作り端で発散、(2)核0付近で Infinity 経由の核0潰れ（復帰不能）。
  `CORE_EPS`（0.05人）で比率を定義しない扱いにし、軸を `stdN`（定員・稼働率のみで決まる法令由来の値）に
  アンカーして固定。**端点を連続40回踏むテスト（ZERO-01）で fail-before/pass-after を実証。**
- 探索1万件が端点を踏まないため見逃していた穴に、端点テストを追加したのは検出力の実質的な向上。
- 指摘C（P3・新規）：軸アンカーを指示の `nMinComp` ではなく `stdN` に変更した。
  nMinComp は職種構成に依存しドラッグで動くため「軸が動かない」要件と両立しない、という理由は
  設計書§11に記録済み。**判断としては妥当だが、指示と異なる実装であることの確認は必要。**

### ④' ブランド・信用 … 🟡
- 版数は本体（title・eyebrow・書き出し）・README とも v0.5 で一致。
- **指摘D（P3・新規）**：README の「index.html 本体。単一HTML・**外部通信なし**」「施設の環境で
  **外部通信を伴わない**こと」という記述と、フッターの forms.gle リンク（外部）が、字面としてずれる。
  実態は「ページは外部資源を読み込まない。問い合わせリンクは利用者のクリックで外部サイトを開く」。
  修正案：README の該当2行に「（問い合わせリンクを除く）」等の限定を付す。**事実の誤りではないが、
  第1回で『外部送信の痕跡なし』を根拠に安全性を述べているため、根拠の表現を現況に合わせるべき。**
- レイアウト：前提と出典が全幅化され縦余白が減った。実測で `.cols` と同幅（1280pxで1140px）。

### ⑤' 法務・規約・表示 … 🟢〜🟡（第1回から改善）
- 免責一文は実装済み（第1回はP3で「独立した免責一文なし」だった）→ **解消**。
- 残件：個人情報を入力しない旨（上記③指摘B）。
- 無料・AI応答なしのため特商法表記・AI利用明示は不要（変更なし）。

### ⑥' 運用・ログ・引き継ぎ … 🟡
- 問い合わせ導線（forms.gle）と版数表示（©行に v0.5）が実装され、**誤りに気づいた利用者が
  水田さんへ届ける経路ができた**（第1回のP3を解消）。
- 完全ブラウザ内でログは残らない（データ保護上は妥当）。
- 公開停止手順：GitHub Pages の Settings → Pages を無効化（〜1分）。**既に公開されているため、
  この手順が実際に必要になり得る点は G1 の判断に含める。**

### ⑦' 教訓照合 … 🟢
- 設計書は「実装しない（スコープ外）」「当時の記録」を枠で明示し、現況と履歴を区別している（E-004準拠）。
- §10 に「計算式を変えるときは説明文を全走査する」手順を制定し、過去3件の見落としを型として記録。
  **手順に「走査結果をレビュー報告に貼る。貼っていなければ未実施とみなす」まで書いたのは妥当。**
- E-003 禁止語（「実装時に報告」「存在すれば」）：設計・報告に無し。
- 本記録も、テスト件数・実測値は実出力の転記（E-001）。

---

## STAGE 3 判定：🟡 条件付き可

- **P0（公開不可の絶対事由）**：**0件**。PII漏えい・外部送信・決済・免責なしの断定診断・
  未開示の制度誤りはいずれも無い。**ツールを取り下げるべき欠陥は見つかっていない。**
- **P1（決定が必要）**：
  - **G1 現況の扱い**：既に公開されている。第1回が前提とした「公開前の判断」を経ずに公開された
    状態になっていないかの確認と、(a)継続 /(b)修正して周知 /(c)取り下げ の決定。
- **P2（修正 or 明示的リスク受容）**：
  - 特養・ユニットの管理栄養士が定員によらず一律1.0（制度精査・第1回から継続）
  - 兼務可職種の満額計上（開示済み・第1回から継続）
- **P3**：
  - 免責一文に「個人情報を入力しない」旨がない（第1回の明示文案から脱落）
  - スマホで免責・問い合わせが結果より前に来る（閲覧順）
  - README の「外部通信なし」と問い合わせリンクの字面のずれ
  - 軸アンカーが指示の nMinComp ではなく stdN（理由は設計書§11に記録済み・確認のみ）
  - スマホ実機（実端末）での確認は未（エミュレーションのみ）

**第1回から解消した項目**：免責一文の欠如（実装）／問い合わせ導線・版数フッターの欠如（実装）／
削除済み70%モデルの説明残存（是正）／配置比率のゼロ除算と描画崩壊（修正＋端点テスト）／
通所の意味のない比率軸（案B）／未検証の目安提示18〜19％（削除）。

## STAGE 4 承認記録（要点）

- **本レビューでは一切コードを変更していない**（判定と材料の提示のみ）。
- **本日の承認依頼一覧へ**：
  1. **G1**：公開済みである現況の扱い（継続／修正して周知／取り下げ）
  2. P2：管理栄養士の定員判定（有識者確認）／兼務の扱い（受容 or 係数化）
  3. P3：免責一文への個人情報の1文追加／スマホの閲覧順／README の外部通信の表現／
     軸アンカー stdN の確認／実端末でのスマホ確認
- 公開明示文（第1回案を現況に合わせて更新）：
  「本ツールは福祉施設の人件費配分を検討するための参考シミュレーションです。配置基準は主要職種の
  要約で、加算要件・自治体条例・兼務・経過措置は反映していません。金額の初期値はサンプル（未検証）で、
  自法人の実額に置き換えてお使いください。派遣職員費は人件費総額の内数として扱います。
  **個人情報は入力しないでください。**推移は収入横ばい・現行の賃金カーブ維持が前提で、報酬改定は
  織り込んでいません。」（太字部分が未実装）

## 本レビューの限界（正直な申し送り）

- 配置基準の正誤（管理栄養士・兼務）は制度精査＝水田さん／有識者の領域。本レビューは
  「要約として不正確な点／開示で足りるかの論点」を挙げるまで。
- 公開状態の確認は公開URLの取得結果に基づく。GitHub の Pages 設定そのものは確認していない。
- スマホはエミュレーション（390×844・touch）での実測で、実端末での確認は未。
- 本記録は判定であって承認ではない。**push・公開操作はしていない。**
