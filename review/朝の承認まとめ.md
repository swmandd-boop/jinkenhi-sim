# 朝の承認まとめ — 2026-07-26

対象：人件費トレードオフ・シミュレーター v0.3（`jinkenhi-sim`）
夜間の自律実行。ブランチ `fix/roken-phantom-shortage`。**push・PR・公開はしていません。**

---

## 1. 夜間に実行したこと（承認不要・報告のみ）

### STEP 1 受け入れ確認 … 成功
- `npm install` → `npm test` → `npm run explore` → `npm run dump` を実行。
- テスト **19 件 PASS / 0 fail**（不変条件14＋UI5）。
- 探索 **10,000 ケース → 違反 0 件**。ダンプ **14 場面 / 25,292 文字** 生成。
- 期待値（19件PASS・探索1万件違反0）に一致。停止事由なし。

### STEP 2 adversarial-review（モード2・画面矛盾探し）… 実施
- Claude側（本体HTML＋ダンプを engine で再計算突合）と codex側（gpt-5.6-terra・ダンプのみ）で
  独立レビュー。仕分けの全体は `review/adversarial-review-結果.md`、codex生出力は
  `review/codex-findings.md`。
- **両方が挙げた指摘：3件**
  1. 老健 起動直後の丸め矛盾（0.0人不足・下限0.1人割れ）→ 本夜修正（下記STEP3）
  2. fit-wage「×1.000」なのに正規年収599.8 → 表示方針の判断が要る（§2へ）
  3. 兼務可職種の常勤換算・満額計上（二重計上の疑い）→ 設計判断が要る（§2へ）
- **片方だけの指摘：6件**（§3）。うち codex の4件は ratio モードで hidden な入力欄
  （年間人件費総額）をダンプが拾ったことによる誤検出で、画面矛盾ではないと判定。

### STEP 3 「両方が挙げた」指摘のみ修正 … 1件修正
- 対象：**老健 起動直後の丸め矛盾**（両方指摘・計算バグ・設計変更を伴わない）。
- 先に回帰テストを追加し**落ちること**を確認 → 修正 → 通ることを確認、の順で実施。
  - 追加テスト：`tests/initial-roster.test.mjs`（INIT-01／INIT-02・全サービス）。修正前は
    老健で失敗（介護 配置21.9 < 基準21.905／n35.9 < 下限36.0）。
  - 修正：ENGINE 区間 `initialRows` の丸めを 四捨五入 → **CEIL（0.1単位切り上げ）** に変更。
    基準どおりの初期配置が各職種で基準以上になり、職種別不足も下限割れも消える。
    UI層に計算は追加していない。
- 修正後 **`npm run check` 通過**：テスト **21 件 PASS**（19＋INIT2）／探索 10,000 ケース
  ×**シード3種**（既定・12345・99999）**違反 0 件**。
- 変更ファイル：`jinkenhi-sim.html`（initialRows）／`engine.mjs`（自動再生成）／
  `tests/initial-roster.test.mjs`（新規）／`review/screen-dump.txt`（再生成）。
- コミット（ローカルのみ・**push なし**）：
  - `8969f07` fix(engine): 起動直後の基準どおり配置を CEIL で置き、老健の丸め矛盾を解消
  - `a487141` docs(review): release-gate v0.3 記録
  - `2476048` docs(readme): 検証の状態を最新化
  - ブランチ：`fix/roken-phantom-shortage`

### STEP 4 release-gate（レビューのみ・公開なし）… 実施 → 判定 🟡 条件付き可
- 機械チェック：外部送信なし／APIキーなし／保存なし／トラッカーなし／console PIIなし。
  完全ブラウザ内・単一HTMLと整合。免責・未検証表記は多数あり。
- 記録は `review/release-gate記録.md`。重点3点の結果は §5・§2 に反映。

---

## 2. 承認をお願いする項目（一括承認表）

| # | 種別 | 内容 | 私の推奨 | 影響範囲 | 承認 |
|---|---|---|---|---|---|
| A | その他 | 修正済み「老健 丸め矛盾」を残す（ローカルコミット `8969f07`）。内容確認のうえ承認 | 残す | ENGINE `initialRows`・老健等の初期配置が基準以上に。老健の初期平均年収 738.3→736.2万 | ☐ |
| B | 設計変更 | §0「不変条件シート」を整備する／既存テスト群（14条＋探索1万件）で代替を受容する、のどちらか | 既存テスト群で代替を受容（別途シート化は次版） | 公開可否の前提。ドキュメントのみ | ☐ |
| C | 設計変更 | 老健の管理栄養士基準を「実利用者数」でなく「入所定員」で判定（定員100で基準0.0→1.0） | 修正する（過少配置＝非適合を助長する側の誤りのため） | ENGINE `roken.build`。配置基準の解釈変更 | ☐ |
| D | 設計変更 | 起動直後の「平均年収665万円（特養80床・基準最小配置）」の見せ方に、最小配置×サンプル単価である旨のインライン注記を足す | 足す | UI表示。誤読（独り歩き）の予防 | ☐ |
| E | 設計変更 | fit-wage「×1.000」なのに正規年収599.8：人件費率の0.1%丸めを見直すか、倍率表示を実値寄せに | 次版で対応（実害小・表示精度のトレードオフ） | UI `fit-wage`／表示 | ☐ |
| F | 設計変更 | 兼務可職種（機能訓練指導員・介護支援専門員）を配置下限に満額計上している点の扱い | 現状維持＋注記強化（係数化は要制度精査） | ENGINE `build`／`stdN`。設計判断 | ☐ |
| G | 公開 | GitHub Pages 公開の可否 | **保留**（B・C・D の判断後） | 対外公開 | ☐ |

※ 種別「数値較正」の項目はありません（基準年収・単価・法定福利費率・賞与月数・非正規賃金水準は
　STOP条項4により夜間は一切触れていません）。

---

## 3. 判断が割れた指摘（片方だけが挙げたもの）

| 指摘 | 挙げた側 | 私の見立て | 反映した場合の影響 |
|---|---|---|---|
| 老健 定員100で栄養士基準0.0（実利用者で判定） | Claude | **実バグ寄り**。過少配置を助長。→ §2-C で承認打診 | ENGINE。定員判定に変更 |
| scale≠1 で「正規計＋非正規計 ≠ 合計」（行1+0≠1.6、footer 33.1+11≠70.6） | codex | 実在の見た目矛盾。ただし修正はスライダーと表の整合方針＝**設計判断**。単独修正は別の不整合を生む | UI表／footer。要design判断 |
| 手入力で下限30に下げると「成立する幅」だが職種別基準は44.1必要 | Claude | 妥当な指摘。verdict が手入力nminと職種別必要量を突合しない設計 | 成立判定ロジック。設計変更 |
| 正規比率が同一画面で「75％」と「75.1％」 | Claude | 表示丸めの桁違い。軽微 | UI表示のみ。次版 |
| 看護段階配置の境界オフバイワン（u=30・50で1段重い） | Claude | 過剰要求側＝安全方向。ダンプでは境界未到達で未表出 | ENGINE境界。次版 |
| 比率ラベル「職員 対 入所者」の語順と数値の向きが逆 | Claude | 表示ラベルの語順。軽微 | UI表示のみ。次版 |
| 年間人件費総額のstale値（23791/4951 等）＝矛盾 | codex×4件 | **誤検出**。ratioモードで当該入力欄は hidden、実使用値は rev×率。ユーザー非表示 | 対応不要（ダンプ材料の改善は §6） |

---

## 4. 実行を止めた項目とその理由（「必ず止まること」該当）

- **push・PR作成・リモート反映・GitHub Pages 有効化**：全て未実施（条項1）。ローカルコミットのみ。
- **本リポジトリ外のファイル**：swmd-brain 等に一切触れていない（条項2）。品質保証フレームワーク
  本体も swmd-brain 内のため未照会（STAGE 1.5 は skill 内の記述で照合）。
- **未検証の初期値（基準年収・単価・法定福利費率・賞与月数・非正規賃金水準）**：一切変更せず（条項4）。
- **codex に渡したファイル**：`review/screen-dump.txt`（本リポジトリ内・サンプル値のみ・クライアント
  情報なし）に限定（条項5）。
- **「両方が挙げた」以外の指摘の反映**：していない（条項6）。§3 は報告のみ。
- **設計方針の変更を伴う指摘**：両方指摘のうち #2（fit-wage表示）・#3（兼務二重計上）は
  設計/表示方針の変更を伴うため未修正（条項7）。§2 で承認打診。

---

## 5. 未検証のまま残っている事項

README「数値の検証状態」は**すべて未検証のまま**（夜間に較正しない方針＝STOP条項4）。

| 項目 | 現在値 | 夜間の検証 |
|---|---|---|
| 人件費率の参考値 | 64.3% | 未検証（据え置き） |
| 1人1日あたり収入 | 特養13,200円 ほか | 未検証（据え置き） |
| 職種別の基準年収 | 介護370万 ほか | 未検証（据え置き） |
| 法定福利費率 | 16.5% | 未検証（据え置き） |
| 賞与月数 | 4.0ヶ月 | 未検証（据え置き） |
| 非正規の賃金水準 | 正規の70% | 未検証（据え置き） |

**夜間に検証できたこと**：計算層の不変条件（21件）・探索（3シード×1万件）・老健修正の回帰。
**夜間に検証できないこと**：上記サンプル値の実額妥当性（実データ較正が必要な別作業）、
配置基準の解釈の正誤（②-C/②看護境界は制度精査＝水田さん／有識者確認が要る）。

---

## 6. 次にやるべきこと（優先順）

1. **§2 の一括承認**（A 修正の受容 → B/C/D の可否 → G 公開判断）。
2. **C（老健栄養士の定員判定）と D（665万の見せ方）** を承認されれば、STEP3 と同じ
   TDD（テスト先行→ENGINE修正→check通過）で対応可能。
3. **B（§0不変条件シート）** の方針決定。既存テスト群で代替受容なら、その旨を README/仕様に明記。
4. `tools/dump-screen.mjs` を hidden 要素除外に改修（今回 codex に4件の誤検出を出させた材料側の
   品質改善。レビューの精度が上がる）。
5. 次版で P3：免責一文の集約・個人情報注意・問い合わせ導線＆版数フッター・正規比率の桁統一・
   比率ラベル語順・看護段階配置の境界。
6. **公開は B/C/D 判断後**。GitHub Pages 有効化は水田さんの操作で。

---

### 報告の但し書き（教訓 E-001/E-002/E-004）
- 「修正済み」と書いた老健の件は、追加テスト（修正前失敗→修正後成功）と `npm run check`
  通過で検証済み。ただしブラウザ実機での目視確認は未実施（jsdom ベースの突合まで）。
- release-gate 判定 🟡 は**公開許可ではありません**。P0相当の論点（§0シート）が残るため、
  公開可否は水田さんの判断が前提です。
- テスト件数（21）・探索件数（1万×3シード・違反0）は実際の出力の転記です。
