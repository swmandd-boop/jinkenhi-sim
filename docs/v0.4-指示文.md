# v0.4 実装指示（Claude Code に貼る）

`docs/v0.4-設計.md` が仕様。以下を順に投げる。**1つずつ**、報告を確認してから次へ。

---

## 指示A — 先行修正2件（v0.4 の前提）

```
敵対的レビューの指摘のうち、⑤と①を修正してください。docs/v0.4-設計.md の
第9節に方針があります。TDD手順（テスト先行 → ENGINE/UI修正 → npm run check → コミット）で進めます。

■ ⑤ スライダー操作時の「正規計＋非正規計 ≠ 合計」

水田さんの判断: footer の差し替えではなく、構造を直します。
スライダーを離した時点で倍率を入力値に書き戻し、scale を常に 1 に戻してください。
ドラッグ中のみ内部倍率を使い、pointerup で確定します。
これで未スケール値とスケール後の値が同時に画面に存在する状態がなくなります。
0.1人単位の丸めが入りますが、常勤換算の粒度として許容します。

先に追加するテスト:
- scale を 0.4 / 0.7 / 1.0 / 1.3 / 1.8 で回し、正規計＋非正規計＝合計 を検証
- 各行でも 正規＋非正規＝合計 を検証
- ドラッグ確定後に scale が 1 に戻り、入力値の合計が確定前の表示と一致すること

注意: v0.3 の UI-01 は scale=1 の1点でしか見ておらず、この不整合を
捕らえられませんでした。同じ穴を再現しないでください。

■ ① 老健の管理栄養士が「定員100で基準0.0」

判定キーを実利用者数 u から入所定員 sz.cap に変更します。
基準の文言「入所定員100以上で1以上」と一致させてください。

先に追加するテスト:
- 老健・定員100・稼働率を 80/90/92/100 と振り、いずれも栄養士の基準が 1 以上
- 定員99では 0 のまま

■ 完了条件
npm run check が通り、ローカルコミットまで。push はしない。
```

---

## 指示B — v0.4 本体

指示Aが通ってから。

```
docs/v0.4-設計.md のとおりに v0.4 を実装してください。
TDD手順を守り、計算は必ず ENGINE 区間に入れます。UI層に計算式を書かないこと。

順番:

1. 削除（第1節）
   基準年収 rows[].a、賃金倍率 k、Bs/Bh、fit-wage ボタン、表の3列。
   テスト INV-08 / INV-09 は削除。
   これで敵対的レビュー指摘④（fit-wage の丸め）は機能ごと消滅します。個別修正は不要。

2. 計算の再定義（第2節）
   avgSe = pool / (nSe + hw*nHi) 、avgHi = avgSe*hw に置き換え。
   削除の前後で avg / avgSe / avgHi / total / pool が変わらないことを
   回帰テストで確認してから削除に進んでください。
   （hw = 0/0.5/0.7/1.0/1.3、scale = 1.3 で一致することは確認済みです）

3. 新機能A・B・C（第5〜7節）
   閾値と既定値を発明しないこと。設計書に書いていない基準値を勝手に置かない。
   特に B の賃金側は atgt が未入力なら判定を出さず、入力を促すこと。
   C の g は既定 0.0％。

4. テスト追加（第8節）
   INV-15 〜 INV-21 を追加。

5. npm run dump を再実行し、review/screen-dump.txt を更新。

■ 完了条件
npm run check が通り、作業ブランチにローカルコミット。push はしない。
報告は review/v0.4実装報告.md に書き、チャットにはパスと結果の要点だけ。

■ 報告の書き方
検証していないことを「完了」「網羅」と書かない。
テスト件数・探索件数は実際の出力をそのまま転記する。丸めない。
未検証のサンプル値（第10節）は変更していないことを明記する。
```

---

## 指示C — release-gate（公開の直前）

v0.4 が固まってから。既存の `CLAUDE_CODE_PROMPT.md` の指示4を使う。
その際、重点3点に次を追加する。

```
④ 推移機能について、収入横ばい・報酬改定を織り込まないという前提が
   利用者に伝わる形で表示されているか。将来予測として独り歩きしないか。
```
