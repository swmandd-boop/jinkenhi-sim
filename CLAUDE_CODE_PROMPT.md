# Claude Code への指示文

以下をそのまま Claude Code に貼る。**1つずつ**投げ、完了報告を確認してから次へ進む。

---

## 指示1：受け入れ確認（まずこれだけ）

```
このフォルダは SWMD 人件費トレードオフ・シミュレーター v0.3 です。
まず README.md を読み、以下を実行して結果を報告してください。変更は加えないこと。

1. npm install
2. npm test        （不変条件14件＋UI5件が通るか）
3. npm run explore （10,000ケースの探索テスト）
4. npm run dump    （review/screen-dump.txt が生成されるか）

報告項目：
- 各コマンドの成否と件数
- 失敗があれば、その不変条件名と再現用入力
- README の「設計の約束」に対して、いま本体HTMLが違反している箇所があるか
  （ENGINE区間へのDOM依存の混入／UI層への計算式の混入）

この時点では修正しないこと。報告だけで停止してください。
```

---

## 指示2：adversarial-review（モード2・画面矛盾探し）

指示1が通ってから実行する。事前に `codex --version` が通ることを確認する。

```
adversarial-review スキルをモード2（画面矛盾探し）で実行してください。

対象：review/screen-dump.txt（14場面の全表示値ダンプ）
Claude側は本体HTMLとダンプの両方を読んでよい。codex側にはダンプのテキストを渡すこと。

レビュー観点として以下を明示的に含めてください。
- 同一画面の数値どうしの食い違い（合計と内訳、比率とその分母、判定文と数値）
- 文言と数値の矛盾（「満たしています」と警告表示の併存など）
- 場面をまたぐ連続性（規模を変えた前後で、基準・下限・実配置の3つが整合しているか）
- 配置基準の解釈そのものの誤り（看護職員の段階配置の境界値、通所介護の常勤換算換算式、
  兼務前提の職種を独立行で数えていることによる二重計上）

指摘は「どの場面のどの2つの値／文言か」を特定して書くこと。
指摘を「両方が挙げた」「片方だけ」に仕分けて報告してください。
この時点では修正せず、報告だけで停止してください。
```

---

## 指示3：指摘の反映（指示2の報告を水田さんが確認したあと）

```
adversarial-review で「両方が挙げた」に分類された指摘のみを対象に修正してください。
「片方だけ」の指摘は、修正案を提示して判断を仰ぐこと（勝手に反映しない）。

修正の制約：
1. 計算に関わる修正は必ず ENGINE 区間に入れる。UI層に計算式を書かない。
2. 修正1件ごとに、それを捕まえる不変条件テストを tests/ に追加する。
   テストを先に書いて落ちることを確認してから直すこと。
3. npm run check が通ることを確認してから報告する。
4. 数値の初期値（基準年収・単価・法定福利費率等）は変更しない。
   これらは未検証のサンプルであることを README に明記済みで、
   実データでの較正は別の作業とする。

報告項目：反映した指摘、追加したテスト、check の結果、判断を仰ぐ項目。
```

---

## 指示4：release-gate（外部公開の直前）

**HP掲載・GitHub Pages公開・研修配布のいずれかを行う前に必ず通す。**

```
release-gate スキーマで jinkenhi-sim.html の公開前レビューを実行してください。

<release_context>
ツール名／バージョン：人件費トレードオフ・シミュレーター v0.3
配布チャネル：GitHub Pages（SWMDサイトからリンク）
想定ユーザー：施設長・事務長・理事（福祉法人の経営層）
扱うデータ：施設の定員・収益・人件費・職員数・賃金水準。利用者・職員個人の情報は入力させない
データの行き先：完全ブラウザ内（外部送信なし）
AI応答の有無：なし
課金の有無：無料
</release_context>

機械チェック（scripts/scan_tool.py）を先に実行し、そのうえで観点レビューを行ってください。
特に次の3点を重点的に見てください。

1. 制度適合：配置基準の記載が指定権者の解釈と食い違わないか。
   未検証・未反映であることの表示が、経営判断に使われる場面で十分か。
2. 誤回答リスク：初期値がサンプルであることに気づかないまま、
   出力された賃金水準を実額として受け取る経路がないか。
3. ブランド毀損：「基準ちょうどの配置なら平均年収665万円」のような
   極端な初期表示が、説明なしに独り歩きしないか。

判定は承認そのものではなく、水田さんが公開ボタンを押すための材料として報告してください。
```
